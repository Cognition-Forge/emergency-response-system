# Emergency Accommodation CLI environment variables
# Copy to .env and adjust values for your environment

# PostgreSQL connection string (TLS recommended)
DATABASE_URL=postgresql://materials_admin:change_me@127.0.0.1:5432/materials_management

# OpenAI API key (required when ai_provider=openai in config/search_parameters.yaml)
OPENAI_API_KEY=sk-your-key-here

# Anthropic API key (required when ai_provider=anthropic in config/search_parameters.yaml)
# ANTHROPIC_API_KEY=sk-ant-your-key-here

# Google API key (required when ai_provider=google in config/search_parameters.yaml)
# GOOGLE_API_KEY=your-google-api-key-here

# Optional overrides
# LOG_LEVEL=INFO
# AI_MODEL=gpt-4o-mini
# AI_TEMPERATURE=0.2
# AI_MAX_OUTPUT_TOKENS=900
# AI_TIMEOUT_SECONDS=12.0
# EARLY_STOPPING_THRESHOLD=5

# LLM Request/Response Logging
# Set to 'true' to log full LLM request/response payloads to logs/llm_calls.jsonl
LOG_LLM_CALLS=false
